# 安全政策

感谢你对本项目安全的关注。请通过负责任的方式报告安全漏洞。

## 报告安全漏洞

若发现安全漏洞，请**不要**在公开 Issue 中披露。请通过 GitHub 的**私有安全通告**（Security → Report a vulnerability）提交，或联系维护者。

报告时请尽量包含：

- 漏洞类型与影响范围
- 复现步骤或最小 PoC
- 受影响的版本
- 你建议的修复方向（如有）

维护者会在确认后尽快响应，并在修复发布后与你确认致谢。

## 凭据与隐私约定

本项目遵循以下安全约定，贡献时请遵守：

1. **不硬编码凭据**：任何 API key、TOKEN、密码均通过环境变量或配置文件读取。在线 PaddleOCR API 的凭据从 `PADDLEOCR_API_TOKEN` 环境变量读取，参考 [`.env.example`](.env.example)。
2. **不收录隐私数据**：仓库不包含真实答题卡图片、真实姓名/学号明细。请勿在 Issue、PR 或提交中附带此类数据。
3. **提交前自查**：提交代码前请确认未包含 `.env`、密钥、个人身份信息（PII）。

## 依赖安全

本项目推理链路依赖 `onnxruntime`、`opencv-python-headless`、`numpy`、`Pillow`、`Flask` 等第三方库。请定期升级到修复了已知漏洞的版本：

```bash
pip install --upgrade -r requirements.txt
```

若发现某个依赖存在安全漏洞，欢迎通过上述渠道报告。

## 支持范围

当前仅对 `main` 分支的最新提交提供安全修复。历史版本不单独维护。
